<!-- note.com 告知記事 / Amical v1.7.1-fork.4 / 日本語のみ・リポジトリにはコミットしない -->
<!-- タイトル案（note のタイトル欄に貼る。本文に重複して入れないこと）:
     1. 音声入力アプリ Amical（オンデバイス版フォーク）に大型アップデート — 辞書ライブラリ・Claude Code 連携・オンデバイス校正
     2. Mac の音声入力を「全部ローカル」で完結させる — Amical フォーク v1.7.1-fork.4
     カバー画像の差し込み位置・スクショ位置は本文中の（▶ スクショ: …）を参照。 -->

Mac の音声入力アプリ **Amical** には、私（veltrea）が公開している「オンデバイス特化フォーク」があります。本家 Amical の良さはそのままに、**音声認識から校正まで全部 Apple Silicon のローカルだけで完結させる**ことに振り切った派生版です。クラウドにも API キーにも依存しません。

今回のアップデート（**v1.7.1-fork.4**）で、大きな機能が一度に3つ入りました。

- 🗂️ **辞書ライブラリ** — 分野別の辞書をワンタッチで足して誤認識を減らす
- 🤖 **MCP サーバー** — Claude Code から語彙や履歴を直接いじれる
- ✍️ **オンデバイス校正** — ローカル LLM が文字起こしを自動で整える

順番に紹介します。

（▶ スクショ: メニューバー常駐 / 録音中のフローティングウィジェット）

## そもそもこのフォークは何？

ひとことで言うと「**完全オンデバイス × AI エージェント連携**に全振りした Amical」です。

本家 Amical は Intel Mac も含めて幅広く動く、よくできた汎用アプリです。一方このフォークは Apple Silicon 専用と割り切って、**Qwen3-ASR（MLX）** という新しめのオンデバイス音声認識を主役に据えています。前回（fork.2）でその音声認識エンジンが入り、今回はその周辺を一気に強化した、という位置づけです。

## 🗂️ 辞書ライブラリ — 分野辞書をトグルで足す

音声認識は、固有名詞や専門用語が苦手です。「鬼滅の刃」「ティアキン」「ルアー」みたいな単語は、放っておくと正しく出てくれません。

今回の **辞書ライブラリ** は、そういう分野ごとの単語をまとめた辞書を**最初から同梱**していて、設定画面でトグルを on にするだけで使えます。

同梱辞書は9個（合計749語）：

| 辞書 | こんな人に |
|---|---|
| オンラインサービス / AI企業 / ソフトウェア・ツール | 仕事で IT 用語をよく喋る人 |
| プログラミング | 開発者 |
| アニメ・マンガ / ライトノベル / ゲーム | 実況・感想を喋る人 |
| 料理 / 釣り | 趣味の話をする人 |

**嬉しいポイント:**

- **辞書単位で on/off**。「今日はアニメ実況するからアニメ辞書だけ on」が一瞬で済みます。
- 切り替えても、**自分で登録した単語には一切触れません**。辞書はバンドルのまま重なるだけで、あなたの語彙リストを上書きしたり汚したりしません。
- 有効にした辞書は、音声認識エンジンが参照する語彙に自動で合成されます。

（▶ スクショ: 辞書ライブラリ設定画面（カードグリッド + カテゴリフィルタ））

## 🤖 MCP サーバー — Claude Code から辞書・履歴を触る

ここがこのフォークらしい部分です。Amical 自身を **MCP サーバー**にして、**Claude Code（や他の MCP クライアント）から Amical の中身を直接操作**できるようにしました。

できること：

- **語彙の追加・編集・重複整理** を Claude にやらせる（`vocabulary_*`）
- **発話履歴の検索・取得**（`transcriptions_*`）

たとえば「最近の文字起こし履歴を見て、よく誤変換されてる固有名詞を辞書に登録しておいて」と Claude に頼む、といった使い方ができます。単語を1個ずつ手で足す時代の終わりです。

**安全面:**

- デフォルトは **OFF**。使うときだけ設定でトグルを on にします。
- 接続は **127.0.0.1（loopback）限定**＋ **Bearer トークン認証**。LAN の他のマシンからは触れません。
- ただし発話履歴は相手の LLM に渡ります。発話履歴には個人的な内容が含まれがちなので、**何を共有するか理解した上で**有効化してください。

Claude Code 側へはコマンド一発で登録できます：

```bash
claude mcp add amical \
  --transport http \
  --url http://127.0.0.1:7878/mcp \
  --header "Authorization: Bearer <トークン>"
```

（▶ スクショ: MCP サーバー設定画面（有効化トグル + ポート + トークン + スニペット））

## ✍️ オンデバイス校正 — ローカル LLM が文字起こしを整える

音声認識の生の出力は、句読点が無かったり、「えーと」みたいなフィラーが混ざったりします。今回の **オンデバイス校正** は、**ローカルの小さな LLM** がその後処理を自動でやってくれる機能です。**クラウドにも、Ollama のような別ソフトにも頼りません**。音声認識と同じ MLX ヘルパーの中で、完全オフラインで動きます。

**ポイント:**

- **言語別のおすすめモデル**を選べます。日本語なら軽量な `LFM2.5-1.2B-JP`（約1.3GB）あたりが第一候補。英語・中国語にもおすすめがあり、Hugging Face の MLX モデルなら好きなものを指定もできます。
- **自分の校正ルール**を文章で書けます。「常に です・ます調にして」「専門用語は英語のままにして」のような指示を足せます。もちろん、勝手に意味を書き換えたり翻訳したりしない安全ルールが常に優先されます。
- **メモリ戦略**（balanced / fast / low）で、LLM をどれだけメモリに常駐させるか選べます。LM Studio など他のローカル LLM と共存させたいときに便利です。

（▶ スクショ: 校正設定画面（モデル選択 + ユーザールールの textarea））

## 📦 入手・インストール

[GitHub Releases](https://github.com/veltrea/amical/releases) から最新の `.dmg` をダウンロードしてください。

- **Apple Silicon Mac 専用**（M シリーズ）。MLX が Apple Silicon 専用のためです。Intel Mac の方は本家 Amical をどうぞ。
- **macOS 15 以降**。
- このビルドは **ad-hoc 署名**（Apple の有料 Developer ID は使っていません）。初回起動時に Gatekeeper が警告したら、**アプリを右クリック → 開く**、または システム設定 → プライバシーとセキュリティ → このまま開く で許可してください。
- 起動後、**マイク**と**アクセシビリティ**の権限を求められたら許可してください。

## 🙏 本家へのリスペクト

このアプリは [Amical](https://github.com/amicalhq/amical)（[amical.ai](https://amical.ai)）を土台にしています。ベースの完成度はすべて本家チームのおかげです。**「幅広く安定して使いたい」なら本家**、**「最新のローカル AI を Apple Silicon で攻めたい」ならこのフォーク**、という棲み分けです。

気に入ったら GitHub で ⭐ をいただけると励みになります。感想・要望もぜひコメントで。
