# Phase 2 詳細設計 — 文脈フィルタ（その場面で「効く語」だけを選んで渡す）

親計画: [CONTEXTUAL_BIASING_PLAN.md](CONTEXTUAL_BIASING_PLAN.md) の Phase 2 をくわしく書いたもの。
作成: 2026-06-07 / 対象: `apps/desktop`（Qwen3-ASR を使うときの処理）

> このドキュメントで何度か出てくる言い方を先に:
> - **「1回の録音」** … ホットキーを押して話し始め、離して文字が確定するまでの1回分。途中で複数の文を話してよい。
> - **「発話の断片」** … その録音の中で、無音でいったん区切られたひとかたまり。Amical は断片ごとに文字起こしする。
> - **「前もって渡す語」** … Qwen3-ASR は、聞き取りを始める前に「こういう語が出るかも」という語のリストを受け取れる（Phase 0 で配線ずみ）。これを多めに渡しても効き目はゆるやかなので、**当たりそうな語を選んで渡す**ほど効く。

---

## 0. このドキュメントの目的

「Phase 2 ＝ 文脈フィルタ」が**具体的に何をするのか**を、例で説明する。
読み終えたら「何を・どこで・どう作るか」が分かる状態を目指す。

---

## 1. 一言で言うと

> **いま入力しようとしている文書・アプリ・話題に「関係する語」だけを選んで、Qwen3-ASR に前もって渡す。**

今は「最近追加した語を 200 個、機械的に」渡しているだけで、**何の話をしているかを見ていない**。
Phase 2 は「**その場面で効く語**」に絞る。こうすると渡す語の 1 つ 1 つの当たり率が上がり、
（前もって渡す語の効き目はゆるやかでも）変換精度が体感で上がる。

---

## 2. なぜ必要か — 今の問題（具体例）

ユーザーが**いくつもの分野辞書を ON** にしているとする（プログラミング / 料理 / アニメ / 法律）。

### 例A: コードを書きながら話す
- 状況: コードエディタで「プルリクをマージしてコンフリクトを解消」と話す
- 今、前もって渡している語（最近の 200 語）: 「みじん切り」「鬼滅の刃」「公証人」「マージ」「プル」…
  - 料理やアニメの語が**無関係なノイズ**として混ざる。せっかくのヒントがぼやける。
  - 最悪、無関係な語にわずかに引っ張られて、かえって変換を乱す。

### 例B: 同音異義語
- 状況: 法律系のアプリで「こうしょうにんの立ち会いが必要」と話す
- 今: 何の話かを見ないので、「交渉人」「公証人」のどちらが出るかは運任せ。

**核心**: 辞書を全部渡す＝薄く広く。Phase 2 は**濃く狭く**。
（親計画 §1.3 のとおり、長すぎる・無関係な語を渡すと、かえって精度が落ちることが研究で分かっている。）

---

## 3. Phase 2 だとどうなるか（同じ例で）

### 例A
- 手がかり: 使っているアプリがコードエディタ（だから話題は「プログラミング」だろうと見当をつける）。さらにカーソル前の文章に「git」「function」が出ている。
- 選んで渡す語: 「マージ」「プル」「コンフリクト」「リポジトリ」「ブランチ」…（プログラミング辞書を優先し、料理・アニメは外す/後回し）。
- 結果: 渡す語が**全部「効く語」**になる。

### 例B
- 手がかり: カーソル前の文章に「契約」「立会」「印鑑」など法律の語がある。だから話題は「法律」だろうと見当をつける。
- 選んで渡す語: 法律辞書の語（「公証」「公証人」「謄本」…）を優先する。
- 結果: 「公証人」が出やすくなる。
  - ※ 同音異義語をきちんと出し分けるのは Phase 3（読みでまとめた辞書）の仕事。Phase 2 は「関係する分野を厚くする」ことで、ついでに良くなる程度。

---

## 4. 「今の話題」を見当づける手がかり（どれも今あるか、取れるもの）

| 手がかり | 中身 | 今どこで取れるか | いつ取れるか |
|---|---|---|---|
| **カーソル前のテキスト** | いま書いている文書の、カーソル直前の部分 | 取得ずみ（[transcription-service.ts:571](apps/desktop/src/services/transcription-service.ts:571) で既に使用） | 録音を始めた瞬間に1回 |
| **使っているアプリ** | 入力先アプリの名前と識別子（例: VSCode、com.microsoft.VSCode） | **取得ずみ**。録音開始時に OS の API で最前面アプリを取っている（[swift-helper](packages/native-helpers/swift-helper/Sources/SwiftHelper/services/AccessibilityContextService.swift)） | 録音を始めた瞬間に1回 |
| **ウィンドウのタイトル・URL** | 前面ウィンドウのタイトル、ブラウザなら開いている URL | **取得ずみ**（URL はブラウザで取れた場合のみ） | 録音を始めた瞬間に1回 |
| **ON にしている辞書の分野** | 有効な辞書のジャンル（一般 / プログラミング / …） | 辞書の一覧情報（[catalog.ts:19](apps/desktop/src/services/dictionary-library/catalog.ts:19)） | 録音の準備時 |
| **直前に話した内容** | さっき認識された文 | [transcription-service.ts:402](apps/desktop/src/services/transcription-service.ts:402) | 録音中、発話の断片ごとに更新 |

> 大事な点: カーソル前のテキストと使っているアプリは、**録音を始めた瞬間に1回だけ読み取られ、その録音が終わるまで変わらない**。
> 直前に話した内容だけは録音中に更新される。これが §6（いつ選ぶか）に効く。

---

## 5. 作る前に知っておく制約

1. **ASR に前もって渡す語のリストは、「1回の録音」ごとに固定される**。録音を始めた時点で一度だけ作られ、その録音が終わるまで、同じリストを最後まで使い回す（作っている場所は [transcription-service.ts:810](apps/desktop/src/services/transcription-service.ts:810)、使い回している場所は [:413](apps/desktop/src/services/transcription-service.ts:413)）。
   - だから Phase 2 の最小形は「**録音を始めた瞬間の手がかりで、語彙を1回だけ選ぶ**」が素直。
2. **今は、辞書から語を取り出すとき「その語がどの分野の辞書から来たか」を捨てている**（[catalog.ts:36](apps/desktop/src/services/dictionary-library/catalog.ts:36) の取り出し結果は、語・言い換え先・言い換えかどうか、だけ）。分野で選ぶには、ここに「分野」を持たせる改修がまず要る。**これが Phase 2 の土台作業**。
3. **アプリ名・識別子・ウィンドウタイトル・ブラウザURL・カーソル前テキストは、すでに録音開始時に取得できている**（材料は揃っている）。未実装なのは「それらから話題を見当づけて語を選ぶ処理」の方。つまり Phase 2 は「材料はある、使い方を作る」段階。

---

## 6. いつ選ぶか

| やり方 | 中身 | 採用 |
|---|---|---|
| **(a) 録音を始めた時に1回** | その瞬間のカーソル前テキスト・アプリ・ON 辞書の分野で語彙を選び、その録音中は固定 | **Phase 2 で採用**。今の作りに乗る・軽い・効果が出る。 |
| (b) 録音中ずっと | 直前に話した内容も使って、途中で語彙を選び直す | Phase 2 の次の段階。カーソル前テキストとアプリは録音開始時に固定なので、別の仕掛けが要る。後回し。 |

---

## 7. どうやって語を選ぶか（点数づけ）

> 候補の語 1 つ 1 つに**点数をつけて、点数の高い順に、渡せる量**（Phase 1 で決めたトークンの上限）**まで詰める**。

点数の付け方（高いほど優先）:

- **手動で登録した語**は常に高くする（今までの方針どおり）。
- **いまの話題（見当をつけた分野）に合う語**は高くする ← これが 2a。
- **カーソル前の文章に出てくる語、またはその語と同じ辞書の語**は高くする ← これが 2b。
- それ以外は、**最近登録した語をすこし優遇**（手がかりが弱いときの保険）。

### 「いまの話題」の見当のつけ方

> くわしい見当づけの仕組み（前の見当を保つ「居座り」・個人の記録・優先順位のはしご）は別ファイル
> [CONTEXTUAL_BIASING_FIELD_ESTIMATION.md](CONTEXTUAL_BIASING_FIELD_ESTIMATION.md) にまとめた。下の 3 つはその土台。

§4 の手がかりを次の 3 つで組み合わせる:

1. **使っているアプリから** — 「どのアプリならどの分野とみなすか」の対応表を用意しておく。たとえば、コードエディタ（VSCode・JetBrains）なら「プログラミング」、チャットやメール（Slack・メール）なら「仕事・一般」、という具合。
2. **カーソル前のテキストから** — そこに各分野の代表語がどれだけ出るか（「git」「関数」が多ければプログラミング、「契約」「印鑑」が多ければ法律）。
3. **ON にしている辞書から** — 有効な辞書の分野のかたより（プログラミング辞書だけ ON なら、まずプログラミング）。

### 「同じ辞書の語を高くする」の補足

ある語がいまの文章に出ていたら、**その語と同じ辞書に入っている他の語**も「関係ありそう」とみなして優先する、という素朴なやり方で初手は十分。

---

## 8. 小さく出して効果を見る（3 段階）

| 段階 | 中身 | 必要な作業 |
|---|---|---|
| **2a 分野でしぼる** | アプリと ON 辞書の分野から話題を見当づけ、その分野の語を優先 | 辞書から語を取り出すときに「分野」も持たせる ／ アプリと分野の対応表 ／ 語を選ぶ処理 |
| **2b 文章との重なりで加点** | カーソル前テキストや直前の話に出る語、その同じ辞書の語を加点 | 2a の選ぶ処理に「重なり」の加点を足す |
| **2c 合算して仕上げ** | 上を重みづけで合算し、保険（最近の語）とまとめる | 重みの調整 ／ 実地テスト |

各段は単独で価値が出る。**2a だけでも例A のノイズは消える**。

---

## 9. 実装の具体（ここはコードを触る人向け）

### 処理の流れ（2a の最小形）

録音を始めると、次の順で進む:

1. カーソル前テキストとアプリを読み取る（既存、[transcription-service.ts:378](apps/desktop/src/services/transcription-service.ts:378)）。
2. 語彙リストを組み立てる（既存の `buildContext`、[:810](apps/desktop/src/services/transcription-service.ts:810)）。この中で:
   1. 有効な辞書から、**分野つきで**語を取り出す（← 取り出し処理を分野つきに拡張）。
   2. 手がかり（見当をつけた分野・カーソル前テキスト）をまとめる。
   3. その手がかりで語を選ぶ（← 新しく作る純関数 `selectContextualHints`）。
   4. 選んだ結果を語彙リストにする。
3. あとは録音中、発話の断片ごとに既存の処理（Phase 0/1 の `buildQwen3Context`）が、その語彙を Qwen3-ASR に渡す。

### 新しく作る純関数（単体でテストしやすいよう切り出す）

```ts
// apps/desktop/src/utils/contextual-hints.ts

// 候補の語 1 つ（辞書 1 エントリ分）
export interface HintCandidate {
  word: string;                   // 語そのもの
  isReplacement: boolean | null;  // 言い換えルールか（true は対象外）
  category?: string;              // どの分野の辞書から来たか
  isManual?: boolean;             // 手動登録の語か
  dateAdded?: Date;               // 保険用の「登録の新しさ」
}

// いまの話題を見当づける手がかり
export interface ContextSignals {
  fieldHint?: string | null;   // 見当をつけた分野（例: "programming"）
  contextText?: string | null; // カーソル前テキスト（＋直前の話）
}

// 候補を点数づけし、優先順に並べた語の配列を返す（純粋な関数）。
export function selectContextualHints(
  candidates: HintCandidate[],
  signals: ContextSignals,
  maxWords = 200,
): string[] {
  /* 点数づけ → 並べ替え → 上位を取る。DB や画面に依存しない。 */
}
```

- 渡せる量での打ち切りは、今までどおり下流の `buildQwen3Context`（Phase 1）が担当。ここでは**優先順に並べる**まで。

### 触るファイル

| ファイル | 何をするか |
|---|---|
| [operations.ts](apps/desktop/src/services/dictionary-library/operations.ts) | 「分野つきで語を取り出す」版を追加（今の関数は壊さない） |
| `apps/desktop/src/utils/contextual-hints.ts` 🆕 | `selectContextualHints`（純関数）＋ アプリと分野の対応表 |
| [vocabulary-hints.ts](apps/desktop/src/utils/vocabulary-hints.ts) | 今の選び方を、新しい選ぶ処理に置き換え |
| [transcription-service.ts](apps/desktop/src/services/transcription-service.ts) の `buildContext` | 手がかりをまとめて、新しい選ぶ処理を呼ぶ |

---

## 10. 確かめ方

- **単体テスト**（純関数なので、アプリ全体を起動せず `tsx`/`vitest` で確認できる）:
  - 話題に合う語が上位に来る
  - カーソル前テキストに出る語が加点される
  - 手動登録の語は必ず残る
  - 話題が分からないとき（手がかりが空）は、今までの「最近の語」順に戻る
- **実地の試し**（副作用なし）: helper だけを単体で動かし、同じ音声に対してカーソル前テキストや分野を変えて、結果の違いを見る。同音異義語や専門語が正しい表記で出る率を見る。
- **アプリ本体での確認は最後**（`install-dev.sh` は普段使いの Amical の権限を一時的に無効化してしまうため、いつやるかはユーザー判断）。

---

## 11. 気をつけること

- **話題の見当が外れると逆効果**: 間違った分野を厚くすると悪化する。ただし手がかりが弱いときの戻し先は「最近の語（中立）」ではなく、**前の見当、それも無ければ個人の記録の一番多い分野**にする（[CONTEXTUAL_BIASING_FIELD_ESTIMATION.md](CONTEXTUAL_BIASING_FIELD_ESTIMATION.md) で更新）。何の記録も無い「生まれて初めて」のときは、中立ではなく**テクノロジー（開発）に寄せる**（このユーザーの主分野。保存済みデータの分類で裏づけ済み）。
- **カーソル前テキストが取れないことがある**: 権限やアプリによっては空。そのときはアプリと辞書の分野だけで進める。
- **しぼりすぎ注意**: 必要な語まで落ちることがある。渡せる量に余裕があれば、最近の語で埋め戻す。
- **言い換えルールは対象外**: ここで扱うのは「ヒント用の語」だけ。決まった言い換え（例「マージ」を必ず「merge」に直す類）は、今までどおり文字起こしの**後**で処理する（前で効かせる手がかりと、後で直す言い換えの 2 段構えは維持）。
- **本家のコードに触れない**: 辞書のデータやアプリと分野の対応表は、こちら側で持つ。

---

## 12. あとで決めること

- アプリと分野の対応表をどう持つか（最初は決め打ち、あとで設定できるように）。
- アプリ名・識別子・ブラウザURL から「分野」へ結びつける対応表をどう作り、どう保守するか（材料は取得ずみ。例: ブラウザURLを見て「github.com → プログラミング」のように判定するのも将来できる）。
- 同音異義語の出し分けを Phase 2 でどこまでやるか（基本は Phase 3 の読み辞書に任せる）。
- 録音中に語彙を選び直す (b) を将来入れるか。
