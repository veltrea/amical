# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Built executables (regenerated by `pnpm build:stt-helper`)
bin/

# Turbo cache (for monorepo)
.turbo/

# macOS
.DS_Store

# Note: vendor/ (prebuilt mlx-swift_Cmlx.bundle) IS committed — the build copies
# it next to the binary and `swift build` cannot regenerate it.
