<!-- note.com 告知記事 / Amical v1.7.1-fork.6 / 日本語のみ・リポジトリにはコミットしない -->
<!-- 公開済みの最新 note 記事は fork.3(オンデバイス校正)まで。本記事は fork.4〜fork.6 で追加された機能をまとめて紹介する。 -->
<!-- タイトル案（note のタイトル欄に貼る。本文に重複して入れないこと）:
     1. オンデバイス音声入力 Amical フォーク、校正のその先へ — 辞書ライブラリと Claude Code 連携（fork.4〜6 まとめ）
     2. Mac の音声入力を「全部ローカル＋エージェント連携」に — Amical フォーク v1.7.1-fork.6
     カバー画像・スクショ位置は本文中の（▶ スクショ: …）を参照。 -->

前回（**v1.7.1-fork.3**）の記事では、音声認識の後段に **MLX 動作のオンデバイス LLM 校正** を入れた話を書きました。

そこから数バージョン進み、**fork.4 〜 fork.6** でいくつも機能が増えています。note ではまだ紹介していなかったので、ここでまとめて紹介します。今回の主役はこの2つです。

- 🗂️ **辞書ライブラリ** — 分野別の辞書をトグルで足して誤認識を減らす（**13辞書・1,140語**）
- 🤖 **MCP サーバー** — Claude Code から語彙や発話履歴を直接操作

（▶ スクショ: メニューバー常駐 / 録音中のフローティングウィジェット）

## おさらい：このフォークについて

このフォークは、本家 **Amical** にいくつかの機能を足した派生版です。fork.2 で **Qwen3-ASR（MLX）**、fork.3 で **MLX 校正** が入り、今回（fork.4〜6）は辞書ライブラリや MCP サーバーなどを足しました。

## 🗂️ 辞書ライブラリ — 分野辞書をトグルで足す（fork.4〜）

音声認識は固有名詞や専門用語が苦手です。「鬼滅の刃」「ティアキン」「減価償却」みたいな単語は、放っておくと正しく出てくれません。

**辞書ライブラリ** は、そういう分野ごとの単語をまとめた辞書を**最初から同梱**していて、設定画面でトグルを on にするだけで使えます。fork.4 で9辞書からスタートし、その後 医療・法律・会計・建築といった分野も加えて、いまは **合計13辞書・1,140語** が入っています。

| カテゴリ | 辞書 |
|---|---|
| 一般 | オンラインサービス / AI企業 / ソフトウェア・ツール / 料理 / 釣り |
| 開発者 | プログラミング |
| クリエイター | アニメ・マンガ / ライトノベル / ゲーム |
| 専門職 | 医療・医学 / 法律・法務 / 会計・税務・金融 / 建築・不動産 |

**嬉しいポイント:**

- **辞書単位で on/off**。「今日はアニメ実況するからアニメ辞書だけ on」が一瞬で済みます。
- **プリセット辞書とカスタム辞書は別レイヤー**。プリセット辞書を有効化しても、自分で登録したカスタム辞書に取り込んだり混ぜたりはしません。文字起こしのときに両者が合成されます。

（▶ スクショ: 辞書ライブラリ設定画面（カードグリッド + カテゴリフィルタ））

## 🤖 MCP サーバー — Claude Code から語彙・履歴を触る（fork.4）

このフォークらしい部分です。Amical 自身を **MCP サーバー**にして、**Claude Code（や他の MCP クライアント）から Amical の中身を直接操作**できるようにしました。定義されているツールは **8つ**です。

**語彙（vocabulary）系 — 6つ**

| ツール | できること | 主な引数 |
|---|---|---|
| `vocabulary_list` | 語彙の一覧を取得（検索つき） | `search` / `limit` / `offset` |
| `vocabulary_add` | 語を1件追加（重複はエラー） | `word` / `replacementWord` / `isReplacement` |
| `vocabulary_update` | id を指定して語を更新 | `id` ほか |
| `vocabulary_delete` | id を指定して語を削除 | `id` |
| `vocabulary_bulk_add` | 複数の語を一括追加（`skip` / `overwrite` を選択） | `entries[]` / `mode` |
| `vocabulary_search` | 語を検索（完全一致 / 前方一致） | `word` / `prefix` |

**発話履歴（transcriptions）系 — 2つ**

| ツール | できること | 主な引数 |
|---|---|---|
| `transcriptions_recent` | 直近の発話を取得（時刻以降に絞れる） | `limit` / `since` |
| `transcriptions_search` | 発話テキストを部分一致で検索 | `query` / `limit` |

**具体的な使い方** — Claude Code で自然文で頼むだけです。たとえば「Amical の最近20件の発話を見て、誤認識っぽい固有名詞を拾って辞書に登録しておいて」と頼むと、Claude は内部で次のように動きます:

1. `transcriptions_recent({ limit: 20 })` で直近の発話を取得
2. 中身を読んで誤認識ペアを判断
3. `vocabulary_bulk_add({ entries: [...], mode: "skip" })` でまとめて登録

「単語を1個ずつ手で足す」作業を Claude に丸投げできる、というわけです。

**安全面:**

- デフォルトは **OFF**。使うときだけ設定でトグルを on にします。
- 接続は **127.0.0.1（loopback）限定**＋ **Bearer トークン認証**。LAN の他のマシンからは触れません。
- ただし発話履歴は相手の LLM に渡ります。発話履歴には個人的な内容が含まれがちなので、**何を共有するか理解した上で**有効化してください。

Claude Code 側へはコマンド一発で登録できます:

```bash
claude mcp add amical \
  --transport http \
  --url http://127.0.0.1:7878/mcp \
  --header "Authorization: Bearer <トークン>"
```

（▶ スクショ: MCP サーバー設定画面（有効化トグル + ポート + トークン + スニペット））

## 🔧 安定性とその他の改善

- **長時間使っても安定**（初期から対処）— 長く使うと音声テキスト変換が重くなる・止まる、変換のたびになぜか古いクリップボードの中身が貼り付けられる、といった問題を初期のバージョンで修正しました。**丸一日つけっぱなしで使っても、これらは起こりません。**
- **語彙の import / export** — 全語彙を JSON にエクスポート、skip / overwrite を選んでインポート。機種移行やバックアップに。
- **権限のワンクリック修復** — ad-hoc 署名アプリは再ビルドで macOS の権限（マイク / アクセシビリティ）が外れることがあります。それを設定画面のボタン一発で入れ直せるようにしました。
- **トレイから設定画面を確実に前面化**（fork.5）— メニューバーのトレイから「コンソールを開く」を選ぶと、他のアプリが前面のときでも Amical が前面に出ます。Dock 非表示で常駐しているとき、トレイが設定画面への唯一の入口になるので効きます。

## 📦 入手・インストール

[GitHub Releases](https://github.com/veltrea/amical/releases) から最新の `.dmg` をダウンロードしてください。

- **Apple Silicon Mac 専用**（M シリーズ）。MLX が Apple Silicon 専用のためです。Intel Mac の方は本家 Amical をどうぞ。
- **macOS 15 以降**。
- このビルドは **ad-hoc 署名**（Apple の有料 Developer ID は使っていません）。初回起動時に Gatekeeper が警告したら、**アプリを右クリック → 開く**、または システム設定 → プライバシーとセキュリティ → このまま開く で許可してください。
- 起動後、**マイク**と**アクセシビリティ**の権限を求められたら許可してください。

このフォークは本家 [Amical](https://github.com/amicalhq/amical)（[amical.ai](https://amical.ai)）をベースにしています。幅広く安定して使いたい方は本家をどうぞ。
